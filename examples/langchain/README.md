# LangChain integration example

Planned: a minimal LangChain agent wrapped with PromptWall middleware,
demonstrating policy enforcement on tool calls and a blocked prompt-injection
attempt. Added during Week 8 of the build (see ../../docs/ARCHITECTURE.md).
